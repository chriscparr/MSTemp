using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TweenToPoint : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	//public void TweenTo (Vector3 desiredPosition, float time = 3, iTween.EaseType = iTween.EaseType.easeInOutElastic) {
 //       iTween.MoveTo(this.gameObject, iTween.Hash("position", pos, "time", speed, "easetype", easeMethod, "oncomplete", "EndOfTween", "orienttopath", true, "lookTime", 5));
    
	//}
}
